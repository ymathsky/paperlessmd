[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.mbstring", "php82Extensions.gd", "php82Extensions.curl", "php82Extensions.zip", "php82Extensions.fileinfo", "php82Extensions.opcache", "apacheHttpd"]

[phases.build]
cmds = []

[start]
cmd = "cp /app/apache.conf /etc/apache2/sites-enabled/000-default.conf && apache2ctl -D FOREGROUND"
